apiVersion: v1
kind: Service
metadata:
  name: store-admin-service
  labels:
    app: store-admin
spec:
  type: LoadBalancer
  selector:
    app: store-admin
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
